\ProvidesPackage{Xiao-slides}

\definecolor{BeamerBlue}{rgb}{0.2,0.2,0.7}


\setbeamertemplate{footline}[frame number]{}
\setbeamerfont{footline}{size=\fontsize{10}{11}\selectfont}
\setbeamertemplate{navigation symbols}{}

\setbeamertemplate{bibliography entry title}{}
\setbeamertemplate{bibliography entry location}{}
\setbeamertemplate{bibliography entry note}{}
\setbeamertemplate{bibliography item}[text]

\usepackage[utf8]{inputenc}
\usepackage{xspace} 
 




% To aligh on both sides 
\usepackage{ragged2e}
\justifying

% hyper link property
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
}

\usepackage{relsize}
\renewcommand*{\UrlFont}{\ttfamily\relsize{-2}}

\usepackage{amsmath}
\usepackage{anyfontsize}
\usepackage{bbm}
\usepackage{mathtools}
\usepackage{physics}



% Define macro for ceil and floor
\newcommand\floor[1]{\lfloor#1\rfloor}
\newcommand\ceil[1]{\lceil#1\rceil}


% Cleveref does not work with beamer
% \usepackage[nameinlink]{cleveref}

% tcolorbox macro
\usepackage[many]{tcolorbox}

\newcounter{GlobalTcboxCounter}

\newtcolorbox{mybox}[2][]
{
  colframe = gray!10,
  colback  = gray!2,
  coltitle = blue!85!black,  
  title    = #2,
  arc=0pt,
  outer arc=0pt,
  boxrule=1.5pt,
  left=0mm,
  right=0pt,
}



\newtcolorbox
[use counter=GlobalTcboxCounter,
% number within=section,
]{LemmaBox}[2][]
{
  title    = Lemma~\thetcbcounter: #2,#1,
  fonttitle=\scshape,
  enforce breakable, % breakable across pages
  colframe = gray!10,
  colback  = gray!2,
  coltitle = BeamerBlue, 
  % colframe = black,
  % coltitle = black,
  % colbacktitle = white, 
  coltext  = black, 
  colback  = white,
  arc=0pt,
  outer arc=0pt,
  boxrule=1.5pt,
  left=0pt, % left margin
  right=0pt % right margin
}


 










